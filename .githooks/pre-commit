#!/bin/sh
# Fast pre-commit check: formatting only, to keep commits quick.
# The full gate runs in the pre-push hook and in CI.
#
# Enable per clone with:  git config core.hooksPath .githooks

set -e

echo "pre-commit: cargo fmt --all --check"
cargo fmt --all --check
